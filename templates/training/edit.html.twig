{% extends 'base.html.twig' %}

{% block title %}Bewerk training{% endblock %}

{% block edit %}
    <h1>Bewerk Training</h1>
    {% if is_granted('ROLE_ADMIN') %}
    {{ include('training/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('training_index') }}" style="float: right">Terug naar lijst</a>
    {% endif %}
    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
