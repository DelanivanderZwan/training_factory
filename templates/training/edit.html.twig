{% extends 'base.html.twig' %}

{% block title %}Edit Training{% endblock %}

{% block edit %}
    <h1>Edit Training</h1>
    {% if is_granted('ROLE_ADMIN') %}
    {{ include('training/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('training_index') }}">back to list</a>
    {% endif %}
    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
